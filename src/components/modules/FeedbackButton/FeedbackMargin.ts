import styled from 'styled-components';

const FeedbackMargin = styled.div`
  display: none;

  @media screen and (max-width: 900px) {
    display: block;
    margin-top: 40px;
  }
`;

export default FeedbackMargin;
